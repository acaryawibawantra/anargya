/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'i.dummyjson.com', 'cdn.dummyjson.com', 'dummyjson.com']
  }
};

export default nextConfig;